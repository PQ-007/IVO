import 'package:flutter/material.dart';

class PvPPage extends StatelessWidget {
  const PvPPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: const [Text("PvP Page")],
        ),
      ),
    );
  }
}
